# Byte compiled
__pycache__/
*.py[cod]
*.pyo

# Virtual environments
.venv/
env/
venv/

# OS files
.DS_Store

# Pytest cache
.pytest_cache/
.remember

# Secrets — never commit API keys (.env.example is the template and stays tracked)
.env

# Local embedding cache (regenerated from data/songs.csv + Gemini)
.cache/